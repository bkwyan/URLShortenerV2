import React, { useState } from 'react';


import {
    HeroContainer
} from './styles/HeroStyle';

const Hero = ({handleURL, onSubmit}) => {

    return(
        <HeroContainer>

        </HeroContainer>
    )
};

export default Hero;